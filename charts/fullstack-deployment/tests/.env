USER="${USER:-changeme}"
NAMESPACE="${NAMESPACE:-solo-${USER}}"
RELEASE_NAME="${RELEASE_NAME:-solo}"

TOTAL_NODES=3

LOG_DIR="${LOG_DIR:-/tmp/solo-testing-logs}"
LOG_FILE="${LOG_FILE:-helm-test.log}"
OUTPUT_LOG="${OUTPUT_LOG:-false}"
