USER="${USER:-changeme}"
NAMESPACE="${NAMESPACE:-fst-${USER}}"
RELEASE_NAME="${RELEASE_NAME:-fst}"

TOTAL_NODES=3

LOG_DIR="${LOG_DIR:-/tmp/solo-charts-logs}"
LOG_FILE="${LOG_FILE:-helm-test.log}"
OUTPUT_LOG="${OUTPUT_LOG:-false}"
